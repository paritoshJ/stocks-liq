import {View, Text} from 'react-native';
import React from 'react';

const LoginScreen = props => {
  return (
    <View style={{backgroundColor: 'red', height: 100, width: 500}}>
      <Text>LoginScreen</Text>
    </View>
  );
};

export default LoginScreen;
